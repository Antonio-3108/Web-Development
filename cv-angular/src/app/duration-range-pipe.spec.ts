import { DurationRangePipe } from './duration-range-pipe';

describe('DurationRangePipe', () => {
  it('create an instance', () => {
    const pipe = new DurationRangePipe();
    expect(pipe).toBeTruthy();
  });
});
